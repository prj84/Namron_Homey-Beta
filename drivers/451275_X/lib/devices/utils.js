'use strict'
const getOptBaseTime = 60 * 60 * 1000; 
const TIP_CHANGED = 'Please go back and WAIT for reinitializing complete，then click `thermostat` icon to launch application again.'
const timeDiffSeconds = 946684800

module.exports = {
  getOptBaseTime,
  TIP_CHANGED,
  timeDiffSeconds
}
